@model PaymentMethod

@{
    ViewData["Title"] = "Create Payment Method";
}

<style>
    .create-container {
        margin-top: 40px;
        max-width: 700px;
        background-color: #1e1e2f;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 6px 20px rgba(0,0,0,0.5);
        color: #f8f9fa;
    }

    .create-title {
        font-weight: 700;
        margin-bottom: 25px;
    }

    .form-label {
        font-weight: 600;
        color: #e0e0e0;
    }

    .form-control {
        background-color: #2c2f33;
        border: 1px solid #444;
        color: #fff;
        border-radius: 8px;
        padding: 10px;
        margin-bottom: 5px;
    }

        .form-control:focus {
            background-color: #2c2f33;
            border-color: #0d6efd;
            box-shadow: none;
            color: #fff;
        }

    .field-validation {
        display: block;
        min-height: 1.25em;
        margin-bottom: 10px;
        color: #ff6b6b;
        font-size: 0.85rem;
    }

    .btn-save {
        background: linear-gradient(135deg, #0d6efd, #0056b3);
        border: none;
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        transition: 0.3s;
    }

        .btn-save:hover {
            transform: translateY(-2px);
        }

    .btn-cancel {
        background-color: #6c757d;
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        margin-left: 10px;
        text-decoration: none;
    }

        .btn-cancel:hover {
            background-color: #5a6268;
        }
</style>

<div class="container create-container">

    <h2 class="create-title">
        <i class="bi bi-credit-card-2-front"></i> Add Payment Method
    </h2>

    <form asp-action="Create" method="post">

        <div asp-validation-summary="ModelOnly" class="field-validation"></div>

        <!-- Method -->
        <label asp-for="Method" class="form-label"></label>
        <input asp-for="Method" class="form-control" placeholder="e.g. Visa, PayPal, Cash" />
        <span asp-validation-for="Method" class="field-validation"></span>

        <!-- Description -->
        <label asp-for="Description" class="form-label"></label>
        <textarea asp-for="Description" class="form-control" rows="3" placeholder="Short description about this method"></textarea>
        <span asp-validation-for="Description" class="field-validation"></span>

        <!-- Buttons -->
        <div class="mt-4">
            <button type="submit" class="btn btn-save">
                <i class="bi bi-save"></i> Save
            </button>

            <a asp-action="Index" class="btn-cancel">
                Cancel
            </a>
        </div>

    </form>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}